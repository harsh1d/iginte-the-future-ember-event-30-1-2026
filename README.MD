```
git add 1.ipynb
git commit -m "Update notebook with latest changes"
```

git add pandas.ipynb
git commit -m "Update notebook with latest changes"

git add test.ipynb
git commit -m "Update notebook with latest changes"
